---
title: "RWorksheet_Mijares#5.Rmd"
author: "Jason LLoyd C. Mijares"
date: "2024-11-06"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(rvest)
library(httr)
library(dplyr)
library(polite)

url <- 'https://www.imdb.com/chart/toptv'

page <- read_html(url)

tv_list2 <- page %>% html_nodes('.ipc-title__text') %>% html_text()

length(tv_list2)


session <- bow(url, user_agent = "Educational")

session

tv_list <- character(0)
links <- character(0)

```
```{r}
tv_list <- read_html(url) %>% html_nodes('.ipc-title__text') %>% html_text

tv_list_sub <- as.data.frame(tv_list[2:26])

length(tv_list)

head(tv_list_sub,50)

##tv_list2 <- read_html(url) %>% html_nodes('<h3.ipc-title__text') %>% html_text

##tv_list2 <- as.data.frame(tv_list2[27:50])

##tv_list2
```

